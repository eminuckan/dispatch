interface:
  display_name: "Test Dispatch Mobile"
  short_description: "Test iOS and Android in Dispatch's Device panel"
  default_prompt: "Use $test-dispatch-mobile to verify the affected native mobile flow through Dispatch's Device panel and AgentDevice."

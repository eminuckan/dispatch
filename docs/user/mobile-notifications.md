# Mobile notifications

Dispatch currently shows agent activity while the mobile app is open. Background push alerts and
ongoing agent activity cards are not available. Dispatch Connect provides account discovery and pairing;
it does not deliver mobile push notifications.

Open the app and reconnect to an environment to see its current threads, results, and approval requests.
Direct pairing, LAN access, and Tailscale also require a live app connection to receive new activity.
